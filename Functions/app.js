مینی پروژه کلاس

function calculate (a,b,operator) {

    if(operator === "+") {
        return a+b;
    }

    if(operator === "-") {
        return a-b;
    }

    if(operator === "*") {
        return a*b;
    }

    if(operator === "/")
        return a/b;
}
    console.log(calculate(10,5,"+"));

تمرین اول

function square(number) {
  console.log(number);
}
square(5);
multiply = 5*5;
console.log(multiply)
